package levels.conf.action;

import levels.conf.LevelsConfig;
import commons.versioning.VersionSender;
import commons.versioning.impl.VersionMenu;

public class LevelMenu {
	public static VersionSender versionSender;
	public static VersionMenu LEVELS_MENU;

	public static void init(LevelsConfig yml) {
	//	versionSender = new VersionSender(yml.getConfiguration());

		// LEVELS_MENU = new VersionMenu(new );
	}
}
